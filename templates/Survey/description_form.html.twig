{% extends 'base.html.twig' %}

{% block title %}Hello AskUserController!{% endblock %}

{% block body %}

    <div class="column is-flex is-align-items-center is-justify-content-center is-two-thirds-desktop is-two-fifths-widescreen">
        <div class="box is-flex is-flex-direction-column has-text-centered" style="height: 100%;width: 100%">

            <form action="{{ path('final_string', {from_inct : from_inct, asset_type : asset_type, intervention : intervention, new_user : new_user, hostname : 'Inconnue'}) }}" name="description_form" id="description_form" method="post" class="is-fullheight-percent is-flex is-justify-content-space-between is-flex-direction-column">
{#                {{ form_start(description_form, {'attr' : {'class' : 'is-fullheight-percent is-flex is-justify-content-space-between is-flex-direction-column'} }) }}#}

            <div class="field">
                <label class="label" for="last_name">Détailler l'intervention :</label>

                <div class="is-flex">
                    <div class="control" style="flex: 1">
                        <div class="is-info is-fullwidth">
                            <textarea id="description_form_infos" name="description_text" class="textarea" placeholder="Entrez les détailles de l'intervention ici..." rows="7"></textarea>
{#                            {{ form_widget(description_form.infos) }}#}
                        </div>
                    </div>
                </div>

            </div>

                <div class="is-flex is-justify-content-space-between">

                    <button class="button is-info is-small js-hostname" onclick="goBack()">
                        <i class="fas fa-chevron-left"></i>
                    </button>

                    <button class="button is-info is-small">
                        <i class="fas fa-chevron-right"></i>
                    </button>

                </div>

                {#                <div class="is-flex is-justify-content-space-between">#}
                {#                    <a href="{{ previous_url }}" class="button is-info is-small">#}
                {#                        <i class="fas fa-chevron-left"></i>#}
                {#                    </a>#}
                {#                    <button class="button is-info is-small">#}
                {#                        <i class="fas fa-chevron-right"></i>#}
                {#                    </button>#}
                {#                </div>#}

            </form>
{#            {{ form_end(description_form) }}#}
        </div>

    </div>





    <script>

        // console.log(textAreaContent);
        let copyBtn = document.getElementById('copy-btn');

        // function CopyToClipboard(containerid) {
        //     if (document.selection) {
        //         var range = document.body.createTextRange();
        //         range.moveToElementText(document.getElementById(containerid));
        //         range.select().createTextRange();
        //         console.log(range);
        //         document.execCommand("copy");
        //     } else if (window.getSelection) {
        //         var range = document.createRange();
        //         range.selectNode(document.getElementById(containerid));
        //         window.getSelection().addRange(range);
        //         document.execCommand("copy");
        //         console.log(range);
        //     }
        // }

        function CopyToClipboard(containerid) {
            let textArea = document.getElementById(containerid);
            // console.log(textArea);
            // let textAreaContent = textArea.innerHTML;
            // console.log(textAreaContent);
            textArea.select();
            textArea.setSelectionRange(0, 99999); /* For mobile devices */
            document.execCommand('copy');
        }

    </script>

{% endblock %}



