{% if survey is defined %}
    {% if survey.chemin|length > 1 %}
        {% if survey.chemin[survey.chemin|length-1] != '/CODE-FINAL' %}
            <div class="column">
                <article class="message is-warning">
                    <div class="message-body has-text-centered">
                        <span class="icon-text is-flex is-align-items-center is-justify-content-center">
                            <span class="icon is-normal">
                                <i class="fas fa fa-info-circle"></i>
                            </span>
                            <span>En cas de demande annulée :</span>
                            <a href="{{ path('canceled') }}" class="button mx-3 is-dark is-outlined">Cliquez ici</a>
                        </span>
                    </div>
                </article>
            </div>
        {% endif %}
    {% endif %}
{% endif %}
